[package]
name = "robot_ui"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.29"
image = { version = "0.25", default-features = false, features = ["jpeg"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
resvg = "0.44"

[profile.release]
opt-level = 3
